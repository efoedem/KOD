{
  "builds": [
    {
      "src": "core/wsgi.py",
      "use": "@vercel/python",
      "config": { "maxLambdaSize": "15mb" }
    }
  ],
  "build": {
    "cmd": "python manage.py collectstatic --noinput"
  },
  "routes": [
    { "src": "/(.*)", "dest": "core/wsgi.py" }
  ]
}